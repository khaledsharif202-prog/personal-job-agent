{
  "name": "personal-job-agent",
  "version": "1.10.0",
  "private": true,
  "description": "Precision-first local job search and application workflow dashboard for Germany",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
